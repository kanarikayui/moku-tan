# moku-tan — TOEIC 英単語・熟語 学習アプリ

TOEIC 頻出の英単語と熟語を 4 択クイズで反復学習する、静的 Web アプリ。
GitHub Pages で公開する。バックエンドは持たず、学習履歴はすべてブラウザの
localStorage に保存する。

UI 表示・コミットメッセージ・コメントはすべて日本語で書く。

---

## 1. 技術方針と制約

- **ビルドツールなし。** 素の HTML / CSS / JavaScript (ES2022 モジュール) のみ。
  npm パッケージ、バンドラ、トランスパイラは導入しない。
- **外部依存ゼロ。** CDN からの読み込み禁止（フォント・アイコン・ライブラリすべて）。
  オフラインでも動作すること。
- **バックエンドなし。** 外部への通信を一切行わない。解析ツール・トラッキングも入れない。
- **公開ルートは `docs/`。** GitHub Pages の "Deploy from a branch → `main` / `/docs`"
  を使う。GitHub Actions によるデプロイは不要。
- **モバイルファースト。** 片手で解ける縦画面レイアウトを基準に、広い画面へ拡張する。

### ローカル確認

ES モジュールは `file://` では読めないため、必ず HTTP サーバー経由で開く。

```sh
python3 -m http.server 8000 --directory docs
# → http://localhost:8000/
```

---

## 2. ディレクトリ構成

```
/
├── CLAUDE.md
├── README.md
├── LICENSE
├── docs/                      # GitHub Pages 公開ルート
│   ├── index.html             # 出題画面
│   ├── history.html           # 履歴画面
│   ├── stats.html             # 統計画面
│   ├── settings.html          # 設定画面
│   ├── css/
│   │   └── style.css
│   ├── js/
│   │   ├── main.js            # 出題画面の DOM 制御
│   │   ├── history-page.js    # 履歴画面の DOM 制御
│   │   ├── stats-page.js      # 統計画面の DOM 制御
│   │   ├── settings-page.js   # 設定画面の DOM 制御
│   │   ├── quiz.js            # 問題生成（誤答選択肢の選定を含む）★純粋関数
│   │   ├── scheduler.js       # 出題順スケジューラ ★純粋関数
│   │   ├── stats.js           # 統計の集計 ★純粋関数
│   │   ├── storage.js         # localStorage I/O とマイグレーション
│   │   ├── config.js          # 既定値・定数
│   │   └── data.js            # 語彙データの読み込みと結合
│   └── data/
│       ├── meta.json          # スキーマ版・収録状況・整備進捗
│       ├── words/
│       │   ├── level-1.json … level-5.json
│       └── phrases/
│           └── level-1.json … level-5.json
├── tools/
│   └── validate-data.mjs      # 語彙データの検証スクリプト
└── tests/
    ├── fixtures.mjs           # テスト用データと決定的な乱数生成器
    └── *.test.mjs             # node --test で実行
```

**★純粋関数**と記した 3 モジュール（`quiz.js` / `scheduler.js` / `stats.js`）は
DOM と localStorage に触れてはならない。状態は引数で受け取り、結果を返すだけにする。
この制約があるため、依存パッケージなしで `node --test` からそのままテストできる。

---

## 3. 語彙データ

### 目標規模

単語 **約 2000 語**、熟語・イディオム **約 500 項目**。**目標数に到達済み**
（level 別の内訳は `docs/data/meta.json` と各ファイルを参照）。
以後の追加・差し替えも 100 項目ずつのバッチで行い、都度検証・コミットする。
進捗は `docs/data/meta.json` の `progress` に記録する。

### 難易度帯（`level`）

| level | 目安スコア | 内容 |
|---|---|---|
| 1 | 〜470 | 基礎業務語彙。Part 1/2 に頻出 |
| 2 | 〜600 | 定型ビジネス語彙。Part 5 の中心 |
| 3 | 〜730 | 契約・人事・物流などの実務語 |
| 4 | 〜860 | 抽象語・派生語・言い換え語 |
| 5 | 900〜 | 低頻度だが差がつく語 |

### エントリのスキーマ

```jsonc
{
  "id": "w0001",                    // w=単語 / p=熟語 + 4桁連番。一度振ったら変更しない
  "type": "word",                   // "word" | "phrase"
  "en": "allocate",
  "ja": ["割り当てる", "配分する"], // 先頭が代表訳。出題時は先頭のみ表示
  "pos": "verb",                    // noun|verb|adj|adv|prep|phrase
  "level": 2,
  "tags": ["finance", "part5"],     // 分野・出題パート。統計の切り口に使う
  "root": "alloc",                  // 語族キー。派生語（allocate/allocation）で共有
  "senses": ["assign-distribute"],  // 意味グループ。同義語どうしで共有する（必須・1つ以上）
  "exclude": ["w0123"],             // 同時に選択肢へ並べてはいけない ID（任意）
  "example": {
    "en": "The manager will allocate the budget next quarter.",
    "ja": "部長は来四半期に予算を配分する。"
  }
}
```

`senses` がこのアプリの要になる。**「どちらも正解ともとれる選択肢」を排除する仕組みが
これ一本に依存している**ため、新しい語を追加するときは既存の `senses` 値を必ず検索し、
同じ意味なら同じキーを再利用する。新しいキーを作る前に `grep` で確認すること。

`senses` のキーは英小文字とハイフンで、意味を表す語を 2 語つなげる
（例: `increase-grow`, `cancel-annul`, `require-demand`, `deadline-duedate`）。

### データ作成時の注意

- **既存の単語帳・問題集をそのまま書き写さない。** 語のリスト自体は事実の集合だが、
  訳語・例文・分類はこのリポジトリのオリジナルとして書き起こす。
- 訳語は 1〜3 個。代表訳は最も TOEIC 的な文脈のものを先頭に置く。
- 例文は TOEIC のビジネス文脈（会議・出荷・請求・採用・設備保守など）に寄せ、
  1 文 12 語程度に収める。
- 熟語は原形で登録する（`take over`, `be subject to`, `in charge of`）。
- 表記ゆれを避ける: 訳語の末尾に句点を付けない。動詞は「〜する」で終える。
  名詞に「〜こと」を付けない。

### 検証

```sh
node tools/validate-data.mjs
```

以下を検査し、1 件でも違反があれば非ゼロ終了する。

1. `id` の重複、`en` の重複（大文字小文字・前後空白を正規化して比較）
2. 必須フィールドの欠落、`level` が 1〜5 の範囲外、未知の `pos`
3. `senses` が空
4. `exclude` の参照先が存在しない
5. **誤答生成可能性**: 各エントリについて、後述の除外条件をすべて満たす誤答候補が
   3 件以上存在すること。満たさないエントリは出題不能なので必ず修正する
6. 代表訳が完全一致する別エントリの存在（`senses` の付け忘れの検出）

---

## 4. 出題ロジック

### 4.1 出題方向

設定で 3 種類から選ぶ。既定は「ランダム」。

- `en2ja`: 英語を提示し、意味（日本語）を 4 択から選ぶ
- `ja2en`: 日本語を提示し、英語を 4 択から選ぶ
- `random`: 1 問ごとにどちらかを選ぶ

統計は方向別にも集計する。

### 4.2 誤答選択肢の選定（曖昧な選択肢を出さないための中核）

正解エントリ `a` に対し、以下を**すべて**満たすものだけを誤答候補とする。
この除外条件は、候補が足りない場合でも決して緩めない。

1. `a.senses` と候補の `senses` の積集合が空であること
2. `a.root !== 候補.root`（派生語どうしを並べない）
3. `a.exclude` に候補が含まれず、候補の `exclude` に `a` が含まれないこと
4. 訳語の衝突がないこと — `a.ja` と候補の `ja` のすべての組について、正規化
   （前後空白・中黒・括弧書きを除去）後に一致・前方一致・後方一致のいずれでもないこと
5. `ja2en` の場合、`en` を正規化した文字列が一致しないこと（綴りゆれ・大小文字）

候補が揃ったら、次の優先度で並べ替えて上位から 3 件を取る。
同順位内はランダムに選ぶ（毎回同じ組み合わせにしないため）。

1. `pos` が一致
2. `type` が一致
3. `level` の差が小さい

3 件に満たない場合は優先度条件のみを段階的に外し、それでも足りなければ
その問題を捨てて次のエントリを抽選し直す。捨てた場合は `console.warn` に
エントリ ID を出す（データ不備の検出手段になる）。

### 4.3 選択肢の並び

4 件（正解 1 + 誤答 3）を **Fisher–Yates シャッフル**で毎回並べ替える。
正解位置に偏りが出ないよう、位置を固定するロジックや「前回と違う位置に置く」
といった補正は入れない。

### 4.4 スケジューラ

グローバルな出題連番 `counter`（1 問出すごとに +1）を基準に、各エントリの
`nextDue`（次に出題可能になる `counter`）を管理する。

**回答後の更新**

```
正解:     streak += 1
          nextDue = counter + min(B * GROWTH^(streak - 1), INTERVAL_MAX)
不正解:   streak = 0
          nextDue = counter + A
スキップ: 不正解と同じ（streak = 0 / nextDue = counter + A）
```

スキップは「答えられなかった」点で不正解と同じなので、出題間隔も重みも不正解と
まったく同じに扱う。ただし計数だけは `wrong` と分けて `skipped` に入れる。
`seen` は増えて `correct` は増えないため、正答率は下がり出題されやすくなる。

`A < B` は定数の制約として `config.js` で検証する（A >= B なら例外を投げる）。

**抽選**

1. 直近 A 問に出題した ID をリングバッファで保持し、そこに含まれるものは
   無条件に除外する（同じ問題の連続を防ぐハードガード）
2. `nextDue <= counter` のものを候補にする。候補が空なら、リングバッファ外で
   `nextDue` が小さい順に上位 20 件を候補にする（フォールバック）
3. 各候補の重みを次式で求める

```
未出題 (seen === 0):
    w = W_NEW

出題済:
    acc          = correct / seen
    overdue      = max(0, counter - nextDue)
    overdueBoost = 1 + min(overdue / A, OVERDUE_CAP)
    missFactor   = 1 + MISS_BOOST * (1 - acc)
    mastery      = streak >= 3 ? MASTERY_DECAY^(streak - 2) : 1
    w            = W_BASE * missFactor * overdueBoost * mastery

w = max(w, 0.05)
```

4. 重み付きランダム抽選で 1 件選ぶ

**未出題語の露出制御**
`W_NEW` が出題済の典型的な重みより大きいため、未出題語は自然に混ざる。
ただし直近 20 問のうち未出題だった割合が 60% を超えている間は `W_NEW` を半減させ、
新出語ばかりが続かないようにする。

この設計により、「間違えた語は A 問後から再出題」「正解した語は B 問後から再出題」
「正答率が低い語ほど高頻度」「連続正解した語は指数的に間隔が伸びる」
「未出題語が常に一定割合で混ざる」を同時に満たす。

### 4.5 既定値（`config.js`）

| 定数 | 既定値 | 意味 | 設定画面で変更 |
|---|---|---|---|
| `INTERVAL_WRONG` (A) | 10 | 不正解語の再出題間隔 | ○ (1–100) |
| `INTERVAL_CORRECT` (B) | 40 | 正解語の再出題間隔 | ○ (2–400) |
| `INTERVAL_GROWTH` | 1.6 | 連続正解での間隔伸長率 | × |
| `INTERVAL_MAX` | 400 | 間隔の上限 | × |
| `W_NEW` | 12 | 未出題語の重み | × |
| `W_BASE` | 3 | 出題済語の基礎重み | × |
| `MISS_BOOST` | 2.0 | 誤答率による増幅 | × |
| `MASTERY_DECAY` | 0.45 | 連続正解 3 回目以降の減衰 | × |
| `OVERDUE_CAP` | 3.0 | 超過分ブーストの上限 | × |

---

## 5. 保存データ（localStorage）

キーは `moku-tan:v1:` 接頭辞で統一する。値は JSON 文字列。

| キー | 内容 |
|---|---|
| `moku-tan:v1:settings` | 出題方向、A / B、出題範囲（level・type）、テーマ |
| `moku-tan:v1:progress` | 出題連番 `counter`、抽選ガード `recent` / `newFlags`、エントリ ID → `{ seen, correct, wrong, skipped, streak, nextDue, lastAskedAt, lastResult }` |
| `moku-tan:v1:stats` | 総計・方向別・日別の集計（いずれも `asked` / `correct` / `skipped`） |

- 各値は `schemaVersion` を持ち、`storage.js` の `migrate()` で旧版から変換する。
  項目の追加だけなら版を上げず、`normalize*()` で既定値（`skipped` なら 0）を補う。
- 読み込みは必ず try/catch し、破損時は既定値へフォールバックして
  画面に「保存データを読み込めなかったため初期化しました」と表示する。
- **ブラウザを更新しても続きから出題できるようにする。** 画面側に学習の状態を持たない。
  `counter` だけでなく抽選ガードの `recent` / `newFlags` も `progress` に保存し、
  起動時に復元する。これがないと、更新直後に直前の語がもう一度出てしまう。
- 語彙データ側で `id` を再利用・付け替えすると学習履歴が壊れる。
  **一度公開した `id` は絶対に変更しない。** 語を削除する場合も `id` は欠番にする。

---

## 6. 画面

### 出題画面 (`index.html`)

- 出題文（英語または日本語）と 4 択ボタン、その下に**スキップ**
- **スキップは 4 択に混ぜない。** 誤答候補ではないので、破線の控えめな見た目で
  4 択の下に別立てにする。押すと不正解と同じ扱いになり、意味と例文を表示する
- **正解したときは止めずに次の問題へ進む。** 判定（○ 正解）だけを出し、
  `CORRECT_ADVANCE_DELAY`（300ms）後に自動で次問へ移る。「次へ」ボタンは出さない。
  この待ち時間は 0 にしない — 答えたタップがそのまま次問の選択肢に当たってしまう
- **不正解とスキップのときだけ語の意味を確認できる。** 正解の語・代表訳・例文（英日）を
  表示し、「次へ」ボタンを出して手が止まるようにする
- キーボードは `1`–`4` で解答、`5` でスキップ。`Enter` / `Space` は「次へ」が
  出ているときだけ効く。キーリピート（`event.repeat`）は無視する
- ヘッダーに **今日の** 出題数 / 正解数 / スキップ数 / 正答率 を常時表示。
  値は画面側で数えず `stats.daily[今日]` から描くので、更新しても 0 に戻らない
- 正誤の伝達は色だけに頼らず、記号（○ / ×）とテキストを併記する
- フィードバック領域は `aria-live="polite"`

### 履歴画面 (`history.html`)

- **直近に出題した語の意味を見直すための画面。** 出題を止めずに済むよう別ページにする
- `lastAskedAt` の新しい順に最大 `HISTORY_LIMIT`（50）件。同じ語は最後に出た 1 件にまとめる
  （語ごとの記録なので、何度出題されていても一覧には 1 行しか出ない）
- 各件に 語・品詞・level・出題回数・正答率・**直前の結果**（○ / × / －）・代表訳・例文（英日）
- 「すべて / 不正解 / スキップ」で絞り込む。判定は `lastResult` で行うので、
  「さっき答えられなかった語」だけを短時間で見直せる
- 抽出は `stats.js` の `recentEntries()` に置く（純粋関数のまま保つ）

### 統計画面 (`stats.html`)

- 総出題数、総正解数、スキップ数、通算正答率、最大連続正解、学習日数
  （スキップは正解に数えないので正答率を下げる。誤答と区別できるよう別に表示する）
- 出題方向別（en2ja / ja2en）の正答率
- `level` 別・`type` 別・`tags` 別の正答率
- 直近 30 日の日別 出題数 / 正答率（外部ライブラリを使わず CSS か インライン SVG で描く）
- 苦手な語 上位 20 件（出題 3 回以上かつ正答率の低い順）
- 学習カバレッジ（出題済 / 全収録数）

### 設定画面 (`settings.html`)

- 出題方向、再出題間隔 A / B、出題範囲（level・type の絞り込み）、テーマ
- A >= B を入力した場合は保存させずエラーを表示する
- **初期化**（それぞれ確認ダイアログを挟む）
  1. 統計のみ初期化 — `stats` を消し、`progress` の出題間隔は残す
  2. 学習進捗を初期化 — `stats` と `progress` の両方を消す（設定は残る）
  3. すべて初期化 — `moku-tan:v1:` の全キーを削除する

---

## 7. コーディング規約

- ES2022 モジュール。`import` / `export` を使い、グローバル変数を作らない
- インデント 2 スペース、セミコロンあり、`const` 優先（再代入する場合のみ `let`）
- **`innerHTML` は使わない。** 語彙データ由来の文字列は必ず `textContent` で入れる
- 関数名・変数名は英語、コメントと UI 文字列は日本語
- 純粋関数モジュール（`quiz.js` / `scheduler.js` / `stats.js`）に副作用を持ち込まない
- 乱数はテスト可能にするため、抽選・シャッフル関数は乱数生成器を引数で受け取れる形にする
  （既定は `Math.random`）
- CSS は `docs/css/style.css` の 1 枚。CSS 変数でテーマ（light / dark）を切り替える

---

## 8. テスト

```sh
node --test "tests/**/*.test.mjs"
node tools/validate-data.mjs
```

コミット前に両方を通す。最低限、次を検証するテストを置く。

- `quiz.js`: 同義語（`senses` 共有）・派生語（`root` 共有）・訳語衝突が
  誤答に混入しないこと。候補不足時に除外条件が緩まないこと
- `quiz.js`: シード固定の乱数で正解位置が 4 箇所に分散すること
- `scheduler.js`: 不正解語が A 問後、正解語が B 問後に再出題可能になること。
  直近 A 問以内の語が選ばれないこと。未出題語が一定割合で選ばれること
- `scheduler.js`: スキップが不正解と同じ間隔・同じ重みになり、`wrong` とは別に
  数えられること
- `stats.js`: 集計値と方向別集計が正しいこと
- `stats.js`: 履歴が出題の新しい順に並び、直前の結果で絞り込めること。未出題の語や
  語彙データに無い ID が混ざらないこと
- `storage.js`: 破損 JSON を読んだときに既定値へフォールバックすること
- `storage.js`: `skipped` を持たない旧データを読んでも 0 として扱えること
- `storage.js`: `recent` / `newFlags` を保存して読み戻せること。持たない旧データや
  壊れた値を読んでも空配列に倒れること

---

## 9. 実装の進め方

1. `config.js` / `storage.js` / `data.js` の骨格と、100 語程度の暫定データ
2. `quiz.js` + `scheduler.js` とそのテスト（データが少なくても検証できる）
3. `index.html` / `main.js` / `style.css` で出題ループを完成させる
4. `stats.js` + `stats.html`、`settings.html`
5. `tools/validate-data.mjs`
6. GitHub Pages を有効化して公開確認
7. 語彙データを 100 件バッチで拡充（単語 2000 / 熟語 500 まで）— **完了**

仕組みを先に完成させてからデータを増やす。データ量が増えてからスキーマを変えると
移行コストが跳ね上がる。

---

## 10. Git 運用

- 作業ブランチ: `claude/toeic-vocab-learning-app-t15wz7`
- コミットメッセージは日本語。1 コミット 1 目的にする
  （例: `スケジューラの重み付き抽選を実装`, `熟語データ level-2 を 100 件追加`）
- コミット前に `node --test "tests/**/*.test.mjs"` と `node tools/validate-data.mjs` を通す
- `docs/data/**` の変更は語彙追加のみのコミットに分け、ロジック変更と混ぜない

## 11. GitHub Pages 公開設定

リポジトリの Settings → Pages で以下を設定する（初回のみ、手動作業）。

- Source: **Deploy from a branch**
- Branch: **`main`** / フォルダ **`/docs`**

公開 URL は `https://kanarikayui.github.io/moku-tan/`。
`docs/` 配下のパス参照はすべて相対パスにする（`/js/main.js` のような絶対パスは
サブディレクトリ公開で壊れる）。
